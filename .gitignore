# Files generated by running the Dynema tutorials on the demo data --
# derived, regenerable, and deliberately NOT shipped (the tutorials create
# them): keep them out of the repository.
*_dynema.vcf.bgz
*.tbi
*.csi
*.dgx
*.log
data/quick_start/main_*
data/quick_start/interaction_*
data/quick_start/total_*
# local annotation files for testing dynema-prepare-bed, too big to ship
data/*.gtf.gz
.DS_Store
